# Development toolchain pin. The workspace floor is the lowest *verified*
# toolchain, 1.97 (the Proton registry crates do not compile below ~1.94;
# probe record in docs/spike-2026-08.md). The CI msrv job pins 1.97.1.
[toolchain]
channel = "1.97.1"
