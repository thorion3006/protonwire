{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Milestone 1 baseline: window control only; every product operation goes through the typed daemon_state command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
