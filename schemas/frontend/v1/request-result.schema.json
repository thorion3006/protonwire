{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RequestResult",
  "description": "Successful request outcomes.",
  "oneOf": [
    {
      "description": "Reply to [`Request::Ping`].",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "nonce": {
              "type": "string"
            }
          },
          "required": [
            "nonce"
          ]
        },
        "result": {
          "type": "string",
          "const": "pong"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::GetState`].",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "state": {
              "$ref": "#/$defs/DaemonState"
            }
          },
          "required": [
            "state"
          ]
        },
        "result": {
          "type": "string",
          "const": "state"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::Disconnect`] / [`Request::Shutdown`] /\n[`Request::Logout`] / [`Request::SubmitCredential`].",
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "const": "acknowledged"
        }
      },
      "required": [
        "result"
      ]
    },
    {
      "description": "Reply to [`Request::ServersList`]: the cached catalog revision,\nserved verbatim (FR-10 — the raw upstream body, never\nrewritten), or all-`None` fields when nothing is cached yet.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "body": {
              "description": "The raw catalog JSON body, byte-for-byte as cached.",
              "type": [
                "string",
                "null"
              ]
            },
            "etag": {
              "description": "The cached revision's `ETag`, for diagnostics.",
              "type": [
                "string",
                "null"
              ]
            },
            "fetched_unix": {
              "description": "When this revision was fetched (Unix seconds); absent when\nno catalog is cached.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            }
          }
        },
        "result": {
          "type": "string",
          "const": "servers"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::ServersRefresh`]: the scheduler's report.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "report": {
              "description": "What the refresh did and what follows.",
              "$ref": "#/$defs/ServersRefreshReport"
            }
          },
          "required": [
            "report"
          ]
        },
        "result": {
          "type": "string",
          "const": "servers-refreshed"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to the login family ([`Request::BeginLogin`],\n[`Request::SubmitTwoFactor`], [`Request::SubmitFidoPayload`]):\nthe next step of the flow, never a silent retry.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "step": {
              "description": "The step's outcome.",
              "$ref": "#/$defs/LoginOutcome"
            }
          },
          "required": [
            "step"
          ]
        },
        "result": {
          "type": "string",
          "const": "login-step"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::RefreshSession`]: the post-refresh status.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "status": {
              "description": "The session's login status after the refresh.",
              "$ref": "#/$defs/SessionStatus"
            }
          },
          "required": [
            "status"
          ]
        },
        "result": {
          "type": "string",
          "const": "login-status"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::GetAccount`]: the account snapshot.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "account": {
              "description": "The account facts.",
              "$ref": "#/$defs/AccountStatus"
            }
          },
          "required": [
            "account"
          ]
        },
        "result": {
          "type": "string",
          "const": "account"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::Select`]: the resolved selection with\nFR-23T's provenance fields end to end. The winner is the\nbest-ranked candidate; a successful reply always carries one\n(every refusal path is an [`RpcError`]). Boxed: the full\nprovenance set is large, and this result enum travels through\nthe session writer — indirection keeps every other variant\ncheap.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "result": {
              "description": "The selection result.",
              "$ref": "#/$defs/SelectionResult"
            }
          },
          "required": [
            "result"
          ]
        },
        "result": {
          "type": "string",
          "const": "selected"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::GroupsList`]: the built-in catalog with the\nregistry's provenance stamps and per-group availability.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "catalog": {
              "description": "The full group catalog (every entry, registry order).",
              "$ref": "#/$defs/GroupsCatalog"
            }
          },
          "required": [
            "catalog"
          ]
        },
        "result": {
          "type": "string",
          "const": "groups"
        }
      },
      "required": [
        "result",
        "data"
      ]
    },
    {
      "description": "Reply to [`Request::GroupShow`]: one group's full definition.\nBoxed alongside [`RequestResult::Selected`] — the detail\ndocument's string set is large, and this enum travels cloned.",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "properties": {
            "group": {
              "description": "The group's details.",
              "$ref": "#/$defs/GroupDetails"
            }
          },
          "required": [
            "group"
          ]
        },
        "result": {
          "type": "string",
          "const": "group"
        }
      },
      "required": [
        "result",
        "data"
      ]
    }
  ],
  "$defs": {
    "AccountStatus": {
      "description": "The account snapshot behind `account --json` (FR-7H): never a\nsecret, never a fabricated fact — absent fields mean unknown or\nnot-yet-wired, per the S6 discipline.",
      "type": "object",
      "properties": {
        "credential_source": {
          "description": "The resolved credential INPUT source (FR-7F).",
          "$ref": "#/$defs/CredentialSourceStatus"
        },
        "login_status": {
          "description": "The daemon account session's login status.",
          "$ref": "#/$defs/SessionStatus"
        },
        "persistence_health": {
          "description": "Persistence health, when the writable-store half reports one\n(ER-18). Absent until S5b/S5c wires the writable store — never\nfabricated.",
          "anyOf": [
            {
              "$ref": "#/$defs/PersistenceHealth"
            },
            {
              "type": "null"
            }
          ]
        },
        "writable_store": {
          "description": "The configured WRITABLE store (the S5b/S5c half).",
          "$ref": "#/$defs/WritableStoreStatus"
        }
      },
      "required": [
        "login_status",
        "credential_source",
        "writable_store"
      ]
    },
    "CredentialSourceStatus": {
      "description": "The resolved credential input source (S5a's two arms).",
      "oneOf": [
        {
          "description": "Values arrive over the IPC surface (the S9 interactive\nprovider).",
          "type": "object",
          "properties": {
            "source": {
              "type": "string",
              "const": "interactive"
            }
          },
          "required": [
            "source"
          ]
        },
        {
          "description": "Read-only import from the systemd credentials directory\n(FR-7F/FR-7J).",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "directory": {
                  "description": "The resolved `$CREDENTIALS_DIRECTORY`.",
                  "type": "string"
                },
                "startup_read": {
                  "description": "What the daemon's startup read of the preferred `session`\ncredential found (recorded once, never re-read mid-run).",
                  "$ref": "#/$defs/CredentialStartupRead"
                }
              },
              "required": [
                "directory",
                "startup_read"
              ]
            },
            "source": {
              "type": "string",
              "const": "systemd"
            }
          },
          "required": [
            "source",
            "data"
          ]
        }
      ]
    },
    "CredentialStartupRead": {
      "description": "The recorded outcome of the daemon's startup read of the systemd\n`session` credential — facts only, never value bytes.",
      "oneOf": [
        {
          "description": "A current, integral FR-7C envelope was readable (the\ntransactional import into the writable store is S5b's; this is\nthe input-half fact).",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "schema_version": {
                  "description": "The envelope's schema version.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                }
              },
              "required": [
                "schema_version"
              ]
            },
            "read": {
              "type": "string",
              "const": "read"
            }
          },
          "required": [
            "read",
            "data"
          ]
        },
        {
          "description": "The read refused; `reason` is the typed refusal's value-free\nsummary (the recorded skip reason).",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "reason": {
                  "description": "Value-free refusal summary.",
                  "type": "string"
                }
              },
              "required": [
                "reason"
              ]
            },
            "read": {
              "type": "string",
              "const": "refused"
            }
          },
          "required": [
            "read",
            "data"
          ]
        }
      ]
    },
    "DaemonState": {
      "description": "Full daemon state snapshot — the authoritative view clients resynchronize\nagainst (PRD FR-127D). Connection details (server, tunnel statistics,\nrequested-vs-applied features) join this struct in Milestone 4+ as\nadditive-optional fields, which are wire-compatible WITHOUT a protocol\nversion bump (old and new documents round-trip — see\n`snapshot_sequence_is_optional_on_the_wire` for the same rule on\n`latest_event_seq`). A version bump is reserved for representation\nchanges that break older readers: renaming or repurposing fields,\nchanging a field's type or semantics, or making a previously-optional\nfield mandatory.",
      "type": "object",
      "properties": {
        "active_owner_uid": {
          "description": "UID of the user who owns the active or most recent connection attempt,\nif any.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "daemon_version": {
          "description": "Daemon build version.",
          "type": "string"
        },
        "latest_event_seq": {
          "description": "Sequence number of the newest event already reflected in this\nsnapshot. Daemons that predate the field omit it; clients treat\n`None` as \"sequence unknown\" and fall back to their gap event.\n\nThe resynchronization race this closes (Codex PR review round 2,\nfinding 1): `GetState` is a separate request, so events can be\npublished while it is in flight and the snapshot can reflect\nsequences newer than the gap event that triggered the resync.\nPairing the snapshot with its own sequence lets the SDK advance\nits cursor coherently instead of replaying older buffered events\nafter the newer snapshot.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "network_integration": {
          "description": "Configured network integration mode (the configured value in Milestone\n1; the resolved adapter once Milestone 5 lands).",
          "$ref": "#/$defs/NetworkIntegration"
        },
        "protocol_version": {
          "description": "Protocol version the daemon speaks.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "vpn_state": {
          "description": "Current VPN state.",
          "$ref": "#/$defs/VpnState"
        }
      },
      "required": [
        "protocol_version",
        "daemon_version",
        "vpn_state",
        "network_integration"
      ]
    },
    "Fido2ChallengeParams": {
      "description": "The WebAuthn ceremony parameters of a FIDO2 challenge (the wire\nmirror of the adapter's reduced challenge).",
      "type": "object",
      "properties": {
        "allow_credentials": {
          "description": "Allowed credential IDs (the account's registered FIDO2 keys).",
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "integer",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0
            }
          }
        },
        "challenge": {
          "description": "The ceremony challenge bytes.",
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint8",
            "maximum": 255,
            "minimum": 0
          }
        }
      },
      "required": [
        "challenge",
        "allow_credentials"
      ]
    },
    "GroupAvailability": {
      "description": "A group's FR-23S availability evaluation.",
      "type": "object",
      "properties": {
        "available": {
          "description": "Whether the group resolves to at least one eligible candidate\nover the current cached catalog.",
          "type": "boolean"
        },
        "reason": {
          "description": "The structured reason when unavailable: `no-catalog` (nothing\ncached yet), `physical-country-required` (FR-23Q),\n`entitlement-composition-missing` (a PF-requiring group with\nthe entitlement seam uncomposed — none exist in the v1 catalog),\nor `no-eligible-server` (the FR-22 report eliminated\neverything).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "available"
      ]
    },
    "GroupDetails": {
      "description": "One group's full definition (`group show`; §7.3B's minimum\npreserved representation, wire form).",
      "type": "object",
      "properties": {
        "connection_overrides": {
          "description": "The remaining catalog-declared overrides, verbatim pairs\n(connection-time parameters — the M4 tunnel composes them).",
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "maxItems": 2,
            "minItems": 2
          }
        },
        "connection_type": {
          "description": "The connection type the target addresses, when the kind does\nnot imply it.",
          "type": [
            "string",
            "null"
          ]
        },
        "immutable": {
          "description": "Built-ins are immutable (FR-23M).",
          "type": "boolean"
        },
        "protocol_override": {
          "description": "The `protocol` override token, when set.",
          "type": [
            "string",
            "null"
          ]
        },
        "selection_authority": {
          "description": "The catalog's selection-authority annotation, when set.",
          "type": [
            "string",
            "null"
          ]
        },
        "sources": {
          "description": "The definition's evidence sources.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "summary": {
          "description": "The summary fields (id, label, origin, entitlement, policy,\noverrides, availability).",
          "$ref": "#/$defs/GroupSummary"
        },
        "target": {
          "description": "The target kind token (`fastest`, `fastest-in-country`,\n`fastest-in-region`, `random`, `secure-core`).",
          "type": "string"
        },
        "target_detail": {
          "description": "The target's parameter when it names one (the country, the\nregion; a Secure Core route renders `CH->SE`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "summary",
        "immutable",
        "target",
        "connection_overrides",
        "sources"
      ]
    },
    "GroupProvenance": {
      "description": "A resolved group's identity and policy provenance (FR-23T's\n`group_id`/`origin`; T-33's status-visible override).",
      "type": "object",
      "properties": {
        "group_id": {
          "description": "The stable namespaced group id.",
          "type": "string"
        },
        "origin": {
          "description": "The group's origin (`proton`|`protonwire`).",
          "type": "string"
        },
        "policy_provenance": {
          "description": "How the effective ranking policy was chosen\n(`catalog-default`|`declared-override`).",
          "type": "string"
        }
      },
      "required": [
        "group_id",
        "origin",
        "policy_provenance"
      ]
    },
    "GroupSummary": {
      "description": "One built-in group as the list surface serves it (FR-23U).",
      "type": "object",
      "properties": {
        "allowed_ranking_overrides": {
          "description": "The request-time ranking overrides the catalog declares.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "availability": {
          "description": "The FR-23S availability evaluation.",
          "$ref": "#/$defs/GroupAvailability"
        },
        "definition_source": {
          "description": "The definition's verification source.",
          "type": "string"
        },
        "entitlement": {
          "description": "What entitlement the group needs.",
          "type": "string"
        },
        "id": {
          "description": "The stable namespaced id (FR-23J).",
          "type": "string"
        },
        "label": {
          "description": "The display label.",
          "type": "string"
        },
        "origin": {
          "description": "`proton` or `protonwire`.",
          "type": "string"
        },
        "ranking_policy": {
          "description": "The catalog-declared ranking policy token.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "label",
        "origin",
        "definition_source",
        "entitlement",
        "ranking_policy",
        "allowed_ranking_overrides",
        "availability"
      ]
    },
    "GroupsCatalog": {
      "description": "The full built-in group catalog reply (FR-23I/U).",
      "type": "object",
      "properties": {
        "catalog_revision": {
          "description": "The group registry's catalog revision stamp.",
          "type": "string"
        },
        "groups": {
          "description": "Every built-in group, registry order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/GroupSummary"
          }
        },
        "taxonomy_revision": {
          "description": "The regional taxonomy's revision identity.",
          "type": "string"
        }
      },
      "required": [
        "catalog_revision",
        "taxonomy_revision",
        "groups"
      ]
    },
    "HardFiltersReport": {
      "description": "FR-22's structured account of where every candidate went.",
      "type": "object",
      "properties": {
        "considered": {
          "description": "Candidates that entered the pipeline.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "stages": {
          "description": "The per-stage counts, in the evaluation order (nonzero stages\nonly).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/StageReport"
          }
        },
        "survivors": {
          "description": "Candidates still eligible after every stage.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "considered",
        "survivors",
        "stages"
      ]
    },
    "LoginBlockedReason": {
      "description": "Stable reasons a login flow stops without a session (ER-17; the\nstrings are recorded in `docs/official-parity.yaml`).",
      "oneOf": [
        {
          "description": "Human verification: no authorized public surface.",
          "type": "string",
          "const": "human-verification"
        },
        {
          "description": "Organization SSO: no authorized public surface.",
          "type": "string",
          "const": "organization-sso"
        },
        {
          "description": "Guest login: not exposed by the pinned adapter on Linux.",
          "type": "string",
          "const": "guest-login"
        },
        {
          "description": "Connection feedback: out of scope for the required flows.",
          "type": "string",
          "const": "feedback"
        },
        {
          "description": "A challenge shape the pinned adapter cannot continue (for\nexample recovery codes).",
          "type": "string",
          "const": "unsupported-challenge"
        }
      ]
    },
    "LoginOutcome": {
      "description": "One step of a login-family flow on the wire (the S0/S4 adapter's\n`LoginStep`): a completed session, a continuation challenge, or a\nfail-closed stop with a stable reason.",
      "oneOf": [
        {
          "description": "The session is authenticated; login is complete.",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "session_id": {
                  "description": "The auth session ID.",
                  "type": "string"
                },
                "user_id": {
                  "description": "The Proton user ID.",
                  "type": "string"
                }
              },
              "required": [
                "user_id",
                "session_id"
              ]
            },
            "step": {
              "type": "string",
              "const": "session"
            }
          },
          "required": [
            "step",
            "data"
          ]
        },
        {
          "description": "A second factor is required before the session is usable.",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "fido2": {
                  "description": "FIDO2 ceremony parameters when the account has registered\nkeys.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/Fido2ChallengeParams"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "totp_enabled": {
                  "description": "TOTP is enabled for the account.",
                  "type": "boolean"
                }
              },
              "required": [
                "totp_enabled"
              ]
            },
            "step": {
              "type": "string",
              "const": "challenge"
            }
          },
          "required": [
            "step",
            "data"
          ]
        },
        {
          "description": "The flow has no authorized public continuation; the stable\nreason is carried for the client surfaces (ER-17).",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "reason": {
                  "description": "The stable refusal reason.",
                  "$ref": "#/$defs/LoginBlockedReason"
                }
              },
              "required": [
                "reason"
              ]
            },
            "step": {
              "type": "string",
              "const": "blocked"
            }
          },
          "required": [
            "step",
            "data"
          ]
        }
      ]
    },
    "NetworkIntegration": {
      "description": "Host network integration mode (PRD 6.6). ProtonWire always owns the tunnel\nand privacy policy; the mode only controls uplink observation and DNS\ncooperation.",
      "oneOf": [
        {
          "description": "Pick NetworkManager or systemd-networkd when either owns the default\nuplink, otherwise native.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Observe netlink directly.",
          "type": "string",
          "const": "native"
        },
        {
          "description": "Cooperate with NetworkManager.",
          "type": "string",
          "const": "network-manager"
        },
        {
          "description": "Cooperate with systemd-networkd.",
          "type": "string",
          "const": "networkd"
        }
      ]
    },
    "PersistenceHealth": {
      "description": "Writable-store persistence health (ER-18). Carried only when the\nwritable-store half is wired and reporting.",
      "oneOf": [
        {
          "description": "Restart persistence is intact.",
          "type": "string",
          "const": "healthy"
        },
        {
          "description": "Restart persistence is at risk; operations requiring durable\nlogin must fail. The reason is value-free.",
          "type": "object",
          "properties": {
            "unhealthy": {
              "type": "object",
              "properties": {
                "reason": {
                  "description": "Value-free failure summary.",
                  "type": "string"
                }
              },
              "required": [
                "reason"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "unhealthy"
          ]
        }
      ]
    },
    "PhysicalCountrySource": {
      "description": "Which FR-23Q source supplied the physical country.",
      "oneOf": [
        {
          "description": "An explicit per-request value (`--physical-country`).",
          "type": "string",
          "const": "explicit-request"
        },
        {
          "description": "The configured `connection_groups.physical_country`.",
          "type": "string",
          "const": "config"
        },
        {
          "description": "The cached Proton user-location country (obtained through Muon\nwhile disconnected; read from the daemon's cache — this request\nperforms no location fetch).",
          "type": "string",
          "const": "cached-location"
        }
      ]
    },
    "PhysicalCountryValue": {
      "description": "The resolved physical country with its source (FR-23T carries both;\nthe country code is coarse location, not an IP — the redaction rules\nfor the fine-grained location payload do not apply to it, and\nFR-23T explicitly requires the value on the selection surface).",
      "type": "object",
      "properties": {
        "country": {
          "description": "The uppercase ISO 3166-1 alpha-2 code.",
          "type": "string"
        },
        "source": {
          "description": "Which source supplied it.",
          "$ref": "#/$defs/PhysicalCountrySource"
        }
      },
      "required": [
        "country",
        "source"
      ]
    },
    "ResolvedSelector": {
      "description": "The selector the request resolved to, after target grammar and\ngroup-definition mapping (FR-23T's \"resolved selector\").",
      "type": "object",
      "properties": {
        "detail": {
          "description": "The kind's parameter when it names one (the country code, the\nstate/city/server/gateway name, the group id; the pinned\nentry/exit countries for `secure-core` render as `CH->SE`).",
          "type": [
            "string",
            "null"
          ]
        },
        "policy": {
          "description": "The effective ranking policy token\n(`official`|`balanced`|`load`|`latency`|`random`).",
          "type": "string"
        },
        "target": {
          "description": "The target kind token: `fastest`, `random`, `country`,\n`countries`, `state`, `city`, `server`, `gateway`, `p2p`,\n`tor`, `secure-core`, or `group`.",
          "type": "string"
        }
      },
      "required": [
        "target",
        "policy"
      ]
    },
    "SelectedServer": {
      "description": "The winning server (FR-23T; FR-23D's both-ends rule for Secure Core\nrides `entry_country`/`exit_country`).",
      "type": "object",
      "properties": {
        "city": {
          "description": "The city, when exposed.",
          "type": [
            "string",
            "null"
          ]
        },
        "entry_country": {
          "description": "The route's entry country (equals the exit outside Secure\nCore).",
          "type": "string"
        },
        "exit_country": {
          "description": "The exit country — the canonical selector.",
          "type": "string"
        },
        "id": {
          "description": "The logical server ID.",
          "type": "string"
        },
        "name": {
          "description": "The display name (`UK#42`, `CH-SE#1`, ...).",
          "type": "string"
        },
        "signals": {
          "description": "The signals that ranked it.",
          "$ref": "#/$defs/WinnerSignals"
        },
        "tier": {
          "description": "The minimum plan tier (0 free .. 3 PM).",
          "type": "integer",
          "format": "int8",
          "maximum": 127,
          "minimum": -128
        }
      },
      "required": [
        "id",
        "name",
        "entry_country",
        "exit_country",
        "tier",
        "signals"
      ]
    },
    "SelectionCatalogProvenance": {
      "description": "The catalog revisions a selection ran against (FR-23T's \"catalog\nrevision\" — disambiguated per the m3-plan U6 reading: the SERVER\ncatalog is the revision field's primary meaning; a group selection\nadditionally carries the group registry's own revision stamp).",
      "type": "object",
      "properties": {
        "group_catalog_revision": {
          "description": "The connection-group registry's catalog revision stamp, present\non group selections only.",
          "type": [
            "string",
            "null"
          ]
        },
        "server_catalog_etag": {
          "description": "The cached server catalog revision's `ETag`, when one is cached.",
          "type": [
            "string",
            "null"
          ]
        },
        "server_catalog_fetched_unix": {
          "description": "When the server catalog revision was fetched (Unix seconds);\nabsent when no catalog is cached (a selection then refuses —\nnever fabricates a revision).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      }
    },
    "SelectionResult": {
      "description": "The full selection result — FR-23T's field set, end to end.",
      "type": "object",
      "properties": {
        "catalog": {
          "description": "The catalog revisions the selection ran against.",
          "$ref": "#/$defs/SelectionCatalogProvenance"
        },
        "feature_difference": {
          "description": "Requested-but-not-applied features. Empty at selection time BY\nCONSTRUCTION for selection-plane features — selection either\nsatisfies them (the winner carries them) or refuses; the\nconnection-plane family (`netshield`, `nat`, `lan-access`,\nprotocol-at-tunnel, the port-forwarding REQUEST) is composed by\nthe M4 tunnel (FR-23E's boundary), and its differences land\nhere from the connection transition, never from a query.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "group": {
          "description": "The group identity and policy provenance, on group selections\nonly.",
          "anyOf": [
            {
              "$ref": "#/$defs/GroupProvenance"
            },
            {
              "type": "null"
            }
          ]
        },
        "hard_filters": {
          "description": "The applied hard filters (FR-22's structured report).",
          "$ref": "#/$defs/HardFiltersReport"
        },
        "physical_country": {
          "description": "The physical country that excluded exits, with its source —\npresent only when the request's semantics used it (FR-23T's\n\"when relevant\").",
          "anyOf": [
            {
              "$ref": "#/$defs/PhysicalCountryValue"
            },
            {
              "type": "null"
            }
          ]
        },
        "requested_features": {
          "description": "The selection-plane features the request carried (rendered\ntokens, required then optional).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "selector": {
          "description": "The resolved selector (target + effective policy).",
          "$ref": "#/$defs/ResolvedSelector"
        },
        "winner": {
          "description": "The winning server.",
          "$ref": "#/$defs/SelectedServer"
        }
      },
      "required": [
        "catalog",
        "selector",
        "hard_filters",
        "winner",
        "requested_features",
        "feature_difference"
      ]
    },
    "ServersRefreshOutcome": {
      "description": "What one catalog refresh did.",
      "oneOf": [
        {
          "description": "A new catalog revision was fetched and committed.",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "etag": {
                  "description": "The new revision's `ETag`, when the API sent one.",
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            },
            "outcome": {
              "type": "string",
              "const": "changed"
            }
          },
          "required": [
            "outcome",
            "data"
          ]
        },
        {
          "description": "The stored revision was still current (an ETag match).",
          "type": "object",
          "properties": {
            "outcome": {
              "type": "string",
              "const": "not-modified"
            }
          },
          "required": [
            "outcome"
          ]
        },
        {
          "description": "The upstream rate-limited the refresh; the suppression\ndeadline in the report governs the next attempt.",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "retry_after_seconds": {
                  "description": "The `Retry-After` delay the API supplied, if any\n(already clamped at the adapter's parse seam).",
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "uint64",
                  "minimum": 0
                }
              }
            },
            "outcome": {
              "type": "string",
              "const": "rate-limited"
            }
          },
          "required": [
            "outcome",
            "data"
          ]
        },
        {
          "description": "The refresh failed; `reason` is a stable, never-secret\ndescription.",
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "reason": {
                  "description": "Stable failure description.",
                  "type": "string"
                }
              },
              "required": [
                "reason"
              ]
            },
            "outcome": {
              "type": "string",
              "const": "failed"
            }
          },
          "required": [
            "outcome",
            "data"
          ]
        }
      ]
    },
    "ServersRefreshReport": {
      "description": "The scheduler's report for one manual refresh (FR-11/FR-13I): what\nthe refresh did, whether this caller joined an in-flight refresh,\nand the pacing facts that follow.",
      "type": "object",
      "properties": {
        "coalesced": {
          "description": "`true` when this caller joined an already in-flight refresh\n(T-25 single-flight coalescing).",
          "type": "boolean"
        },
        "next_eligible_unix": {
          "description": "The next automatic eligibility the refresh set (Unix seconds).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "outcome": {
          "description": "What the refresh did.",
          "$ref": "#/$defs/ServersRefreshOutcome"
        },
        "suppression_until_unix": {
          "description": "The active suppression deadline after the refresh, if any\n(ER-16: no path may bypass it).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "outcome",
        "coalesced",
        "next_eligible_unix"
      ]
    },
    "SessionStatus": {
      "description": "Login status of the daemon's account session (the wire mirror of\nthe adapter's status).",
      "oneOf": [
        {
          "description": "No session exists.",
          "type": "string",
          "const": "logged-out"
        },
        {
          "description": "A session exists and is usable.",
          "type": "string",
          "const": "logged-in"
        },
        {
          "description": "A session exists but must be refreshed before use.",
          "type": "string",
          "const": "needs-refresh"
        }
      ]
    },
    "StageReport": {
      "description": "One hard-filter stage's accounting (FR-22's structured report, wire\nform; stages with zero eliminations are omitted).",
      "type": "object",
      "properties": {
        "eliminated": {
          "description": "Candidates this stage eliminated.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "stage": {
          "description": "The stage's stable label (the selection core's evaluation-order\nvocabulary, e.g. `offline`, `target-geography`,\n`physical-country-exclusion`, `required-features`).",
          "type": "string"
        }
      },
      "required": [
        "stage",
        "eliminated"
      ]
    },
    "VpnState": {
      "description": "High-level VPN state machine states.",
      "oneOf": [
        {
          "description": "No tunnel exists.",
          "type": "string",
          "const": "disconnected"
        },
        {
          "description": "A connection attempt is in progress.",
          "type": "string",
          "const": "connecting"
        },
        {
          "description": "A tunnel is up.",
          "type": "string",
          "const": "connected"
        },
        {
          "description": "A teardown is in progress.",
          "type": "string",
          "const": "disconnecting"
        }
      ]
    },
    "WeightedBreakdownWire": {
      "description": "The per-term decomposition of a balanced ranking (FR-16's formula,\nwire form; lower is better).",
      "type": "object",
      "properties": {
        "feature_match_term": {
          "description": "`feature_weight × (1 − match_ratio)`.",
          "type": "number",
          "format": "float"
        },
        "history_term": {
          "description": "Zero until connection statistics exist (post-M4).",
          "type": "number",
          "format": "float"
        },
        "latency_term": {
          "description": "`latency_weight × normalized_latency`.",
          "type": "number",
          "format": "float"
        },
        "load_term": {
          "description": "`load_weight × normalized_load`.",
          "type": "number",
          "format": "float"
        },
        "stability_term": {
          "description": "Zero until connection statistics exist (post-M4).",
          "type": "number",
          "format": "float"
        },
        "total": {
          "description": "The weighted sum (lower is better).",
          "type": "number",
          "format": "float"
        }
      },
      "required": [
        "load_term",
        "latency_term",
        "stability_term",
        "feature_match_term",
        "history_term",
        "total"
      ]
    },
    "WinnerSignals": {
      "description": "The scoring signals behind the winning candidate (FR-23T/FR-14:\nstatus must identify the policy AND the signal provenance).",
      "type": "object",
      "properties": {
        "latency_ms": {
          "description": "The latency observation, when one served the ranking\n(milliseconds).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "load": {
          "description": "The Proton-exposed load percentage, when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int8",
          "maximum": 127,
          "minimum": -128
        },
        "proton_score": {
          "description": "The catalog's opaque Proton score, when exposed.",
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        },
        "provenance": {
          "description": "The signal provenance: `catalog-only` (Proton-exposed catalog\nfields alone), `probe-observed` (plus the bounded on-demand\nprober's latency), or `weighted-breakdown` (the balanced\ndecomposition).",
          "type": "string"
        },
        "weighted": {
          "description": "The balanced decomposition, when the balanced policy ranked the\nwinner.",
          "anyOf": [
            {
              "$ref": "#/$defs/WeightedBreakdownWire"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "provenance"
      ]
    },
    "WritableStoreStatus": {
      "description": "The configured writable-store facts (S5b/S5c own the resolution).",
      "type": "object",
      "properties": {
        "declared": {
          "description": "The declared `account.writable_session_store` vocabulary value.",
          "type": "string"
        },
        "priority": {
          "description": "The configured `account.writable_store_priority` order.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "declared",
        "priority"
      ]
    }
  }
}
